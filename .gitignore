# OS / editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Python / MLX local runs (if labs land in-repo later)
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/

# Model weights & adapters (large; keep local)
adapters/
fused_model/
mlx_model/
*.safetensors
*.npz
*.gguf
*.bin
*.pt
*.npz.index

# Local data with possible PII — never push real statements
data/private/
*.csv
!samples/**/*.jsonl

# Secrets
.env
.env.*
*.pem

# Logs / caches
*.log
.cache/
__mlx_cache__/
