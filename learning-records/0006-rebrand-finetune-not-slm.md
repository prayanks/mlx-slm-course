# Mission shifted: fine-tune for txn labels (not “SLM course”)

User rejected “SLM” branding as misleading. The real goal is fine-tuning an open model (MLX/mlx-lm, LoRA) so transaction text is classified into tax-relevant labels. Course name, mission, lessons, repo, and Pages rebranded accordingly. Small models remain bases we fine-tune — not the course title.
